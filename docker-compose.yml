services:
  pantry:
    build:
      context: .
      args:
        API_URL: ${API_URL:-http://localhost:3000}
    container_name: pantry
    ports:
      - "3000:3000"
      - "80:80"
    volumes:
      - pantry-data:/app/data
    restart: unless-stopped

volumes:
  pantry-data:
